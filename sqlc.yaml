version: "2"

sql:
  - engine: "postgresql"
    schema:
      - "migrations/*.up.sql"
    queries:
      - "sql/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
